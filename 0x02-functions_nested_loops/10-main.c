#include "main.h"
#include <stdio.h>

/**
 * add - add two numbers from input
 * @a: first aparamet
 * @b: second parameter
 *
 * Description: adds two numbers
 * Return: Always (0).
 */

int add(int a, int b)
{
	int n;

	n = add(89, 9);
	printf("%d\n", n);
return (0);
}
